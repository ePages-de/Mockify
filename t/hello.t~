package t::youtTest;
use base t::TestBase;
use strict;
use Test::More;

sub testPlan{
	my $self = shift;	
	$self->testSomething();
	return;
}

sub testSomething {
	my $self = shift;
	my $SubTestName = (caller(0))[3];
	ok(1,"$SubTestName - tests if ...");
	return;
}

__PACKAGE__->RunTest();

1;
